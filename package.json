{
  "name": "courtside-mobile",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vite build && node --test",
    "probe:preprocess": "node tools/preprocess-probe.mjs",
    "probe:still": "npm run build && node tools/still-probe.mjs"
  },
  "dependencies": {
    "onnxruntime-web": "^1.22.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
